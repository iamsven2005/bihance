generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
model User {
  clerkId String @default(uuid()) @id
  userid String
  name String
  profileImage String
  email String
}